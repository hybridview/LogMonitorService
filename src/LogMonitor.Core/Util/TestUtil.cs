using System;
using System.Collections.Generic;
using System.Linq;
using System.Text;

namespace LogMonitor.Core.Util
{
    class TestUtil
    {

        /*
private void SendTestData()
{
           
    UdpClient myUdpclient = new UdpClient();
    try
    {
        IPEndPoint iep = new IPEndPoint(IPAddress.Broadcast, _port);
        byte[] bytes = System.Text.Encoding.UTF8.GetBytes(textBox1.Text);
        myUdpclient.Send(bytes, bytes.Length, iep);
        textBox1.Clear();
        myUdpclient.Close();
        textBox1.Focus();
    }
    catch (Exception err)
    {
        MessageBox.Show(err.Message, "Failed");
    }
    finally
    {
        myUdpclient.Close();
    }
}
*/

    }
}
